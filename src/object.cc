#include "object.hh"


int main(){

    
    let a = object;
    Object b;
    b[values 1, 3.14, "hello", true, &a];
    b.printObject();

    Object c;
    c[  key("a") = 5,
        key("b") = 3.14,
        key("c") = "hello"];
    

    return 0;
}

